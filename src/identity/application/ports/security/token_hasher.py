from typing import Protocol


class TokenHasher(Protocol):
    """Порт для криптографического хеширования токенов.

    Используется слоем приложения для получения детерминированного
    криптографического хеша токена без привязки к конкретному алгоритму
    или библиотеке.

    Реализация должна обеспечивать одинаковый результат для одинаковых
    входных данных и секретного ключа, если алгоритм его использует.
    """

    def hash(self, token_str: str) -> str:
        """Вычислить криптографический хеш токена.

        Parameters
        ----------
        token_str : str
            Исходная строка токена.

        Returns
        -------
        str
            Строковое представление вычисленного хеша.
        """
        ...
