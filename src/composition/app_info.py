import tomllib

from src.composition.paths import BASE_DIR

APP_NAME = "Elyria Backend"
"""Название приложения."""


def _load_app_version() -> str:
    """Читает версию приложения из ``pyproject.toml``.

    Returns
    -------
    str
        Версия из поля ``project.version`` или ``0.0.0-unknown``, если файл
        не найден или версия в нём отсутствует.
    """
    _pyproject_path = BASE_DIR / "pyproject.toml"
    try:
        with open(_pyproject_path, "rb") as _f:
            return tomllib.load(_f)["project"]["version"]
    except FileNotFoundError, KeyError:
        return "0.0.0-unknown"


APP_VERSION: str = _load_app_version()
"""Текущая версия приложения.

Берётся из поля ``project.version`` в ``pyproject.toml``.
Если файл не найден или версия в нём отсутствует, используется значение
``0.0.0-unknown``.
"""

APP_DESCRIPTION = """_Личный цифровой сад наших отношений — место для воспоминаний,
мечтаний и маленьких секретов вдвоём._

### ✨ Особенности

- 📸 Приватный медиа-архив с автоматической сортировкой по датам и событиям;
- 💌 Тайные записки и заметки разных типов: от вишлистов до благодарностей друг другу;
- 🎮 Мини-игры для пар, чтобы не скучать, узнать друг друга получше и создать воспоминания;
- 🔒 Защищённое уютное местечко в интернете: система поддерживает современные методы
шифрования и защиты информации.

### 🌱 Планы по развитию

- 📱 Мобильное приложение c push-уведомлениями и интеграцией с календарём;
- 🔑 End-to-end шифрование медиа и заметок;
- ❤️ Генератор "истории любви" на основе данных, сохранённых в системе."""
"""Полное описание приложения в формате Markdown.

Используется как ``description`` при создании экземпляра
:class:`fastapi.FastAPI` и отображается в OpenAPI-документации.
"""

APP_SUMMARY = "Романтичное приложение-подарок на 14 февраля от Лёши для Светы."
"""Краткое описание приложения для OpenAPI-тегов и превью."""

ADMIN_NAME = "Ванюков Алексей"
"""Имя ответственного лица для поля ``contact`` в OpenAPI."""

ADMIN_EMAIL = "alexeivanyukov@yandex.ru"
"""Email для связи с ответственным лицом для поля ``contact`` в OpenAPI."""
