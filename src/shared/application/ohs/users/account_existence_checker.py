from collections.abc import Sequence
from typing import Protocol, runtime_checkable
from uuid import UUID


@runtime_checkable
class AccountExistenceChecker(Protocol):
    """Порт проверки существования активных учётных записей.

    Определяет контракт OHS контекста Users для других bounded contexts:
    позволяет выяснить, какие из переданных идентификаторов учётных
    записей не соответствуют состоянию "существует и активна".

    Порт оперирует исключительно примитивами (``UUID``) и не раскрывает
    внутреннюю модель контекста Users: он является published language
    интеграции между ограниченными контекстами.

    Notes
    -----
    Операция является чтением: не изменяет состояние и не присоединяется
    к транзакции вызывающей стороны. Реализация обязана считать запись
    отсутствующей, если в источнике истины нет строки с указанным
    идентификатором либо признак активности ``is_active`` равен
    ``False``.
    """

    async def which_do_not_exist(self, identity_ids: Sequence[UUID]) -> list[UUID]:
        """Определить идентификаторы учётных записей, которых нет.

        Возвращает подмножество ``identity_ids``, для которого
        не существует активной учётной записи: запись отсутствует
        либо деактивирована.

        Parameters
        ----------
        identity_ids : Sequence[UUID]
            Идентификаторы учётных записей для проверки.

        Returns
        -------
        list[UUID]
            Идентификаторы, для которых активной учётной записи
            не существует, в порядке следования во входной
            последовательности. Пустой список, если все записи
            существуют и активны.

        Notes
        -----
        Пустая входная последовательность корректна и возвращает
        пустой список.
        """
        ...
